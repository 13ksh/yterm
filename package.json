{
  "name": "kpixel-youtube-filter",
  "version": "1.5.2",
  "private": true,
  "description": "Hide YouTube channels flagged t by del.kpixel.net from feeds, comments, posts, and Shorts. Search stays visible.",
  "scripts": {
    "test": "node --test test/*.js",
    "icons": "python3 scripts/make-icons.py"
  }
}
